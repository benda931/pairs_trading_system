### Plan

- Add small global constants for:
  - canonical phase definitions (`phase_architecture_env`, `phase_observability_ops`);
  - deterministic priority ranking (`PRIORITY_RANK`).
- Make `cmd_plan`:
  - inject missing global phases into the phases list;
  - sort phases by `order` and files by `rel_path` before persisting, for deterministic plans;
  - optionally snapshot previous plan JSON/MD before overwriting (snapshot mode).
- Strengthen idempotency and determinism of file upgrades:
  - `backup_file` will no longer overwrite an existing `.gpt_backup`;
  - `upgrade-file` and `upgrade-all` will skip write/backup when the generated code equals current content.
- Implement snapshot mode for upgrades:
  - per-run snapshot directory under a user-specified `--snapshot-root`;
  - snapshot original files there before any write, without overwriting earlier snapshots.
- Add helper utilities:
  - `generate_run_id`, `compute_snapshot_run_dir`, `snapshot_before_write`;
  - `group_files_by_phase` to tie files to global phase ids.
- Add `run_system_health_check` to integrate with `health_check_full_system.py` (graceful fallback).
- Add a new CLI subcommand `upgrade-phase` that:
  - upgrades only files associated with a given